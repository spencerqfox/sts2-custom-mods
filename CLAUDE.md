When building, utilize a build.ps1 powershell script if available. Request to run it OUTSIDE of the sandbox.

To install the relevant mods, utilize the powershell script if available. Request to run it OUTSIDE of the sandbox.